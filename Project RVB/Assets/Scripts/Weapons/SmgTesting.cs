using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SmgTesting : Weapons
{
    // Start is called before the first frame update
    void Start()
    {
        WeaponStart(2,30, 90, 3.2f,0.45f,0.8f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
